on windows running the server locally:
Set-ExecutionPolicy Unrestricted
A

create two terminals and enter these line
frontend terminal:
cd frontend
(npm install)
(npm install node)
npm start
npm audit fix (--force)
npm run build
(serve -s build -l 80)

backend terminal:
python -m venv venv 
venv\Scripts\activate
cd backend
python Service\app.py


frontend server url:
http://localhost:3001/

backend server url:
http://localhost:5001/


http://nlp-cetrification.cs.bgu.ac.il/