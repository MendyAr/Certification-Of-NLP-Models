using vs. code,
(download node. js)

on windows running the server locally:
Set-ExecutionPolicy Unrestricted
A

create two terminals and enter these line

frontend terminal:
cd frontend
nvm install node
npm install
npm start

backend terminal:
cd backend
pip install flask
pip install sqlalchemy
pip install torch
pip install pandas
pip install scikit-learn
pip install IPython
pip install overrides
pip install typeguard
pip install transformers
pip install seaborn
pip install --upgrade google-auth

python -m venv venv 
venv\Scripts\activate
python app.py


url:
http://localhost:3001/